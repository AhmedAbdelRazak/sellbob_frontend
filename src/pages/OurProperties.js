import React from "react";
import styled from "styled-components";

const OurProperties = () => {
	return (
		<OurPropertiesWrapper>
			<div>Hello From OurProperties Page</div>
		</OurPropertiesWrapper>
	);
};

export default OurProperties;

const OurPropertiesWrapper = styled.div`
	min-height: 1400px;
`;
